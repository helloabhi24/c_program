#include <stdio.h>

int main()
{
   int a,b=1,i;
    printf("Enter a no.");
    scanf("%d",&a);
    while(a>=1)
    {
        b=b*a;
        a--;
    }
    printf("your Factorial is :%d",b);
    return 0;
}
